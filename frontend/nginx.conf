server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so client-side routing (or a hard refresh on any path)
    # doesn't 404 against nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
